language: rust
sudo: false
env:
  global:
    - RUST_BACKTRACE=1
cache: cargo

matrix:
  include:
    - rust: nightly
      script:
        - cargo build
        - cargo test --all
    - rust: stable
      script:
        - cargo build
        - cargo test --all
